apiVersion: v1
kind : Service
metadata:
  name: front-end-service
  labels:
    app: word-press-app
spec:
  type: NodePort
  selector:
    app: word-press-app-frontend
  ports:
  - protocol: TCP
    port : 4000
    targetPort: 80
    nodePort: 31333

