apiVersion: v1
kind : Service
metadata:
  name: database-service
  labels:
    app: word-press-app
spec:
  type: ClusterIP
  selector:
    app: word-press-app-database
  ports:
    - protocol:
      port : 3306
      targetPort: 3306
